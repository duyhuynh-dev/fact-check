# EC2 Backend Environment Variables
# Copy this to /opt/fact-check/.env on your EC2 instance

APP_ENV=production
DATABASE_DSN=sqlite:///./factcheck.db

# API Keys
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here  # Optional

# Configuration
VERIFICATION_PROVIDER=gemini
CLAIM_EXTRACTOR=spacy
FREE_MODE=false

# CORS - JSON array or comma-separated list of allowed origins
# Supports wildcards for Vercel: *.vercel.app will match all Vercel deployments
# Example (JSON): CORS_ORIGINS=["https://*.vercel.app","http://localhost:3000"]
# Example (comma-separated): CORS_ORIGINS=https://*.vercel.app,http://localhost:3000
CORS_ORIGINS=["https://*.vercel.app","http://localhost:3000","http://127.0.0.1:3000"]

# Paths
INGEST_BUCKET_PATH=./data/uploads
PROCESSED_TEXT_PATH=./data/processed
VECTORSTORE_PATH=./vectorstore

# Optional: Redis for job queue
# QUEUE_BACKEND=arq
# REDIS_DSN=redis://localhost:6379


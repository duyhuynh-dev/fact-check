// Vercel configuration for injecting environment variables into HTML
// This file is used by Vercel to replace placeholders in HTML files

module.exports = {
  // This will be handled by Vercel's build process
  // Environment variables are injected at build time
};
